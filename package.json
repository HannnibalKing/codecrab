{
  "name": "codecrab",
  "version": "0.2.0",
  "private": true,
  "description": "Dependency-free local-first project task dashboard",
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}